"""Расчётное ядро ОРБИТЫ.

Пакет не импортирует HTTP-фреймворк, ORM, Redis, MinIO и фронтенд (ADR-001):
api и worker вызывают его как обычную библиотеку.
"""

from typing import Final

# Версия ядра входит в ключ переиспользования готового Run вместе с config_hash (ADR-011),
# поэтому она объявлена здесь, а не собирается из метаданных дистрибутива: значение должно
# быть доступно и при запуске из исходников, без установленного пакета.
ENGINE_VERSION: Final[str] = "orbita-core-0.1.0"

__all__ = ["ENGINE_VERSION"]
