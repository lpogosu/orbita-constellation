# 15. Состояние проекта на 2026-09-13

| Статус | Обновлён | Заменяет | Агенту |
|---|---|---|---|
| Действует | 2026-09-13 | — | Точка остановки перед сессией с экспертом: что сделано, что можно показать, что осталось. Обновлять после каждого milestone |

## 1. Где остановились

Собран и проверен весь backend первого среза: расчётное ядро, API, воркер, хранилища,
полный пользовательский цикл через OpenAPI без интерфейса. Интерфейс ждёт макетов из
Figma; шесть экранов уже нарисованы, требования к остальным — в `14_SCREENS.md`.
Экономика посчитана с источниками (`13_ECONOMICS.md`).

| Milestone | Карточки | Состояние |
|---|---|---|
| M0 Freeze | M0-A, M0-B, M0-C | принято, слито |
| M1 Ядро | M1-A, M1-B, M1-C, M1-D, M1-E | принято, слито; ревьюер M1: три блокирующих замечания закрыты (CLI в документации, детерминированный perf-тест, история без служебных следов) |
| M2 Платформа | M2-A, M2-B, M2-C, M2-D | принято, слито; ревьюер M2 — см. §5 |
| M4 Outage Detective | M4-A | принято, слито: сравнение перерывов и маршрутов до/после, затронутые клиенты, первый момент расхождения |
| M5 Исследования | M5-A | начата, остановлена: в ветке `task/M5-A` сетка sweep в ядре с тестами и черновик репозитория экспериментов |
| M6 Устойчивость | M6-A | начата, остановлена: в ветке `task/M6-A` черновик мостов и модуля устойчивости |
| M3 Интерфейс | — | ждёт макетов; `web/` — nginx со статической страницей |

## 2. Что работает (всё в `main`)

### Ядро `core/orbita_core`
- Валидация сценария `cosmo-A-1.0` со всеми ошибками списком и JSON path; канонизация,
  `config_hash`.
- Векторизованная геометрия по формулам кейса на всех 720 отсчётах; contact plan как
  bitset «отсчёты × рёбра». Сверка с официальным `geometry.py`: расхождение позиций
  9·10⁻¹³ км, множества рёбер совпадают.
- Граф отсчёта, Union-Find как независимая проверка достижимости, три политики
  маршрутизации (`bfs_shortest`, `persistent`, `dijkstra_distance`), вершинно-
  непересекающиеся резервные пути и минимальный разрез (max-flow).
- Диагностика причин разрыва с доказательствами, метрики по клиенту и конфигурации,
  интервалы перерывов с пометкой краевых, ранжирование и рекомендация.
- Экспорт `cosmo-A-result-1.0`, повторная загрузка, детерминизм байт в байт.
- CLI: `run`, `golden`, `crosscheck`, `validate`. Все 48 golden-значений
  `10_FIXTURES.md` §1 сходятся без подгонки; сутки сценария из 48 аппаратов — около
  0,25 с.

### Платформа `api`, `worker`, `deploy`
- Семь сервисов в compose, `/api/health`, degraded-профиль (расчёт без Redis, Memgraph,
  MinIO через локальные адаптеры).
- Postgres: проекты, варианты с diff и происхождением, запуски, метрики, перерывы,
  артефакты; миграции 0001–0003.
- Воркер arq: стадии расчёта, прогресс по SSE, отмена, `Idempotency-Key`, дедупликация
  по `config_hash + engine_version`.
- Адаптеры MinIO (trace.bin, export.json, evidence.zip) и Memgraph (контакты интервалами,
  lineage) с локальными заменами.
- Endpoint результата: snapshot, timeline, metrics, outages, backup-paths, export,
  evidence-pack, comparisons, recommendation, preview. Полный цикл проходит на четырёх
  сценариях кейса и на скрытом сценарии с другими идентификаторами.

### Проверки
- Ядро: 385 тестов (golden, сверка с эталоном, property-тесты против NetworkX, инварианты
  `10_FIXTURES.md` §2). API: 129 модульных и интеграционных, включая e2e на живом стеке.
  Воркер: 8. `ruff` и `mypy --strict` чистые во всех пакетах.
- История: 90 коммитов, один автор, conventional commits на русском.

## 3. Что показать эксперту

Всё ниже выполняется из чистого клона, без интерфейса. Команды подробно — в `README.md`.

1. **Ядро за секунду.** `cd core && python -m orbita_core golden ../scenarios` — таблица
   «ожидание / факт» по 48 значениям; `python -m orbita_core run
   ../scenarios/03_satellite_outages.json --out r.json` — метрики и время расчёта.
2. **Сверка с официальным модулем.** `python -m orbita_core crosscheck
   ../scenarios/01_full_constellation.json --ticks 0,120,43200,86280`.
3. **Ошибки с путём до поля.** `python -m orbita_core validate
   core/tests/fixtures/multi_errors.json` — три ошибки списком.
4. **Полный цикл через API** (`README.md`, раздел «Полный цикл через API»): `make up` →
   загрузка `scenarios/hidden_like.json` → запуск → стадии по SSE → snapshot на отсчёте с
   причиной разрыва и доказательствами → экспорт → повторная валидация экспорта →
   второй вариант с изменённым RAAN → сравнение → рекомендация → evidence pack.
5. **Degraded-режим.** `make degraded` → `/api/health` с `degraded_mode: true` → тот же
   цикл проходит без Redis, Memgraph и MinIO.
6. **Скрытый сценарий жюри.** Всё выше на `hidden_like.json`: 5 плоскостей, 35
   аппаратов, 2 шлюза, 4 клиента, отказ шлюза.
7. **Экономика.** `python docs/economics/model.py` печатает все таблицы
   `13_ECONOMICS.md`; раздел «Для слайда» — главные числа с источниками.

## 4. Что осталось

| Что | Карточка | Оценка |
|---|---|---|
| Эксперименты sweep: воркер, endpoint, материализация точки | M5-A (продолжить ветку) | 1 карточка |
| Рекомендация по кандидатам из экспериментов, evidence pack с рекомендацией | M5-B | 1 карточка |
| Критичность аппаратов, мосты, двусвязные компоненты | M6-A (продолжить ветку) | 1 карточка |
| Lineage с дельтами метрик из Postgres и Memgraph | M6-B | 1 карточка |
| Лимиты, таймауты, `make demo` из чистого клона, публичный URL | M7 | 1 карточка |
| Интерфейс: каркас, пять экранов, карта и таймлайн | M3-A…M3-D по `14_SCREENS.md` | после макетов |

Порядок после возобновления: ревьюер M2 (если не завершён) → `M5-A + M6-A` (продолжение)
→ `M5-B + M6-B` → ревьюер → M7 → интерфейс.

## 5. Итоги волны M4 и ревью M2

**M4-A принята.** `POST /api/comparisons` отдаёт по клиенту новые, исчезнувшие и
изменившиеся перерывы с причинами и отказавшими аппаратами, число отсчётов с сохранённым
и перестроенным маршрутом, первый момент расхождения. На паре «полная группировка —
десять отказов» дельты совпали с `10_FIXTURES.md` §1. Это основа экрана «Отказы».

**Ревьюер M2: принят, блокирующих замечаний нет.** Независимый пересчёт на официальном
модуле совпал с системой по всем 48 значениям; 8640 маршрутов четырёх экспортов
проверены на существование рёбер без нарушений; полный цикл на скрытом сценарии,
degraded-режим, негативные фикстуры и соответствие OpenAPI документу подтверждены.
Три блокирующих замечания ревью M1 закрыты.

Не блокирующие замечания и куда они идут:

| Замечание | Действие |
|---|---|
| `ComparisonEntry` в `05_API.md` расходился с ответом (`is_base` нет, есть `variant_title`, `routing_policy`) | исправлено в документе |
| `05_API.md` §4 противоречил §1 про `Run.degraded_mode` | исправлено в документе |
| `GET /projects/{id}/lineage` отдаёт 200 с пустыми дельтами, хотя Run уже есть | M6-B |
| `api/tests/conftest.py` делает `TRUNCATE` базы из `ORBITA_TEST_DATABASE_URL` без защиты: прогон тестов на адрес живого стека стирает данные | M7: защита по имени базы или отдельная тестовая БД |
| `POST /api/runs` переиспользует Run по `config_hash` глобально, а сравнение и рекомендация отбирают кандидатов внутри проекта | M7: дедупликация в пределах проекта или копия связи Run с вариантом |
| `worker/tests` не покрывают `runner.py` напрямую | M7: модульные тесты отмены, прогресса, ошибок |
| абсолютный путь в `reports/M1-E.md` | исправлено |

## 6. Вопросы к эксперту

1. Краевые перерывы: мы включаем их в `max_gap_s` с флагом `truncated_by_horizon`
   (ADR-004). Приемлемо ли это для отраслевого прочтения «учитываются отдельно»?
2. Как сегодня выполняется перебор конфигураций и сколько часов занимает один вариант?
   Это единственное допущение, которое двигает окупаемость (`13_ECONOMICS.md` §6).
3. Сколько вариантов на исследование и исследований в год — порог окупаемости
   112 вариантов в год.
4. Рекомендация при недостигнутой цели: показывать ближайшую точку и расстояние до цели
   или только честное «цель не достигнута»?
5. Срок хранения расчётов и требования к размещению данных (облако или контур).
6. Какая из функций второй волны ценнее для защиты: перебор конфигураций (sweep) или
   критичность аппаратов?
